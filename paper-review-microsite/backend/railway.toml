[build]
builder = "DOCKERFILE"
dockerfilePath = "paper-review-microsite/backend/Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[[services]]
name = "paper-review-backend"
