## Seed aggregation task (multi-seed roll-up)
- Your job is to aggregate results across seeds and produce a rolled-up report + plots.
- Load each seed run's `experiment_data.npy` from the provided `exp_results_dir` paths below.
- Compute mean ± std/sem across seeds where applicable and produce summary plots (error bars, aggregated curves).
- Save aggregated results into `./working/experiment_data.npy` so a separate metrics-parsing pass can compute final/best metric values.
- Write plots as `.png` files into `./working/`.
- Ensure the aggregation succeeds and produces plots; the runner determines plot success from the artifacts in `./working/`.

{% if seed_nodes is defined and seed_nodes %}
### Seed runs to aggregate

| Seed | exp_results_dir | metric | is_buggy |
|------|-----------------|--------|----------|
{% for seed in seed_nodes %}
| {{ seed.id[:8] }} | {{ seed.exp_results_dir or "N/A" }} | {{ seed.metric or "N/A" }} | {{ seed.is_buggy }} |
{% endfor %}

**Experiment data paths:**
{% for seed in seed_nodes %}
{% if seed.exp_results_dir %}
- `{{ seed.exp_results_dir }}/experiment_data.npy`
{% endif %}
{% endfor %}
{% endif %}
