# ==============================================================================
# Required for Running Experiments (launch_stage1_only.py)
# ==============================================================================

# OpenAI API key (required for LLM queries)
OPENAI_API_KEY=sk-your-openai-key

# HuggingFace token (required for downloading datasets and models)
# Get your token from: https://huggingface.co/settings/tokens
HUGGINGFACE_HUB_TOKEN=hf_your_token
HUGGINGFACE_API_KEY=hf_your_token
HF_TOKEN=hf_your_token

# ==============================================================================
# Optional Variables
# ==============================================================================

# Custom OpenAI-compatible endpoint (e.g., local LLM server, RunPod)
# Only set this if NOT using the default OpenAI API
# OPENAI_BASE_URL="https://your-custom-endpoint.com/v1"

# Anthropic API key - only required if using Claude models
# (e.g., when using bfts_config_claude-haiku.yaml)
# ANTHROPIC_API_KEY=your-anthropic-key

# Semantic Scholar API key (optional, for citation gathering)
# S2_API_KEY=your-semantic-scholar-key

# ==============================================================================
# Orchestrator / Control Plane Variables (for full system deployment)
# ==============================================================================

# MongoDB connection (not needed for standalone experiments)
# MONGODB_URL=mongodb+srv://username:password@cluster.example.com/ai-scientist
# MONGO_DB_NAME=ai-scientist

# Control plane URL (not needed for standalone experiments)
# CONTROL_PLANE_URL=https://your-control-plane.example.com

# MinIO object storage (not needed for standalone experiments)
# MINIO_ENDPOINT=
# MINIO_ACCESS_KEY=
# MINIO_SECRET_KEY=
# MINIO_BUCKET=ai-scientist-artifacts
# MINIO_SECURE=true

# AWS credentials (not needed for standalone experiments)
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_REGION_NAME=

# Google Gemini API key (not needed for standalone experiments)
# GEMINI_API_KEY=
